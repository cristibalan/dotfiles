#!/bin/bash
if [ -z $1 ];then
  ~/Apps/must-have/MacVim.app/Contents/MacOS/Vim -g -u ~/.vimrc -U ~/.gvimrc --servername CHELU
else
  ~/Apps/must-have/MacVim.app/Contents/MacOS/Vim -g -u ~/.vimrc -U ~/.gvimrc --servername CHELU --remote-tab-silent $*
fi
